# Repository Instructions for AI Coding Agents

Read the active feature specification, plan, and task list before changing implementation files.
Preserve the dependency direction documented in `docs/architecture.md`, do not commit secrets, and
avoid logging raw Discord data.

Before reporting implementation complete, update affected documentation, run `pnpm check`, and
report any validation failure honestly. Make focused commits only after the user approves them.
