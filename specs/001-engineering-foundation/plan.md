# Implementation Plan: Engineering Foundation

**Branch**: `001-engineering-foundation` | **Date**: 2026-08-30 |
**Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/001-engineering-foundation/spec.md`

## Summary

Create a production-ready TypeScript background worker that receives Discord Gateway voice-state
events, safely records an observable handling outcome, and exposes liveness and readiness endpoints.
Use a real Discord client adapter in production and a controlled simulated Gateway at the adapter
boundary for integration and end-to-end tests. The repository supplies reproducible local commands,
layered tests, CI quality gates, and contributor documentation.

## Technical Context

**Language/Version**: TypeScript 5.x on Node.js 24 LTS

**Primary Dependencies**: discord.js (Gateway client), Fastify (health/readiness HTTP surface), Zod
(configuration validation), Pino (structured logging), Vitest (tests), ESLint and Prettier

**Storage**: N/A — no event, user, or configuration persistence in the foundation

**Testing**: Vitest unit, integration, and end-to-end suites; deterministic simulated Discord client
through the Gateway factory port; coverage reporting for unit and integration suites

**Target Platform**: Linux container or process-hosted background worker; local macOS/Linux
development

**Project Type**: Background worker service with an operational HTTP interface

**Performance Goals**: Reach ready state within 30 seconds of valid configuration and a reachable
Gateway; process the representative simulated voice-state event within 5 seconds in end-to-end tests

**Constraints**: Node 24 LTS; only the `GuildVoiceStates` Discord Gateway intent; no real Discord
credentials or live Discord dependency in CI; tokens and raw Discord IDs/payloads must never appear
in logs, metrics, or test snapshots; production Discord IDs and payloads must never appear in
committed files, while synthetic test identifiers are permitted only in test fixtures

**Scale/Scope**: One worker process, one Discord application, no sharding, persistence, queue,
database, product commands, or voice-media connection in this feature

## Constitution Check

*GATE: Passed before Phase 0 research and re-checked after Phase 1 design.*

| Principle | Plan evidence | Status |
|---|---|---|
| Testability | Pure handler unit tests; adapter/readiness integration tests; simulated-Gateway E2E | Pass |
| Quality gates | `pnpm check` runs format, lint, typecheck, all test layers, and build locally and in CI | Pass |
| Explicit architecture | Domain, application, ports, and infrastructure have one-way dependencies | Pass |
| Documentation | README, architecture, testing, contributing, and agent guidance are implementation deliverables | Pass |
| API contracts | Health/readiness and metrics response semantics are documented in `contracts/health.md` | Pass |
| Security | Validated configuration and inbound event normalization; least Gateway intent; no secret logging | Pass |
| Observability | Structured safe logs plus Gateway-state and event outcome metrics | Pass |
| Reproducibility | Corepack-pinned pnpm, committed lockfile, frozen CI install, shared commands | Pass |
| Definition of Done | Tasks will require all checks, docs, and no outstanding requirements | Pass |

## Project Structure

### Documentation (this feature)

```text
specs/001-engineering-foundation/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── health.md
└── tasks.md                         # Generated by $speckit-tasks
```

### Source Code (repository root)

```text
src/
├── application/
│   └── handle-voice-state.ts         # Pure use case
├── config/
│   └── load-config.ts                # Environment parsing and redaction-safe errors
├── domain/
│   └── voice-state.ts                # Transient domain types and validation
├── infrastructure/
│   ├── discord/                      # discord.js Gateway adapter
│   ├── http/                         # Liveness, readiness, and metrics server
│   └── logging/                      # Pino observation adapter
├── ports/                            # Gateway, clock, readiness, and observation interfaces
├── composition/
│   └── root.ts                       # Dependency wiring only
└── main.ts                           # Process lifecycle and graceful shutdown

tests/
├── unit/
├── integration/
├── e2e/
└── support/
    └── gateway-simulator/

docs/
├── architecture.md
└── testing.md

.github/workflows/
└── ci.yml
```

**Structure Decision**: A single service retains a small, explicit dependency flow:
`domain ← application/ports ← infrastructure/composition`. Domain and application code must not
import Discord, HTTP, environment, logger implementation, or test-framework modules. No database,
repository, event bus, plugin system, or generic abstraction is introduced because the foundation
does not need one. `DiscordGatewayEventSource` depends on a `DiscordClientFactory` port: production
composition supplies a `discord.js`-backed factory, while CI and end-to-end composition supply a
deterministic simulated client through the same port. The simulator emits ready, voice-state,
disconnect, and reconnect signals without opening a live network connection.

## Complexity Tracking

No constitution violations or complexity exceptions require justification.
